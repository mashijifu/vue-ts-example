import Vue from "vue";
import Vuex from "vuex";
// import user from "@/store/use-store";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
  // modules: { user: user },
});
